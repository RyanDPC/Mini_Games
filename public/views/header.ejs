
<nav>
    <div class="logo-container">
        <img src="/assets/Logo.png" alt="Mini Games Store Logo" class="logo">
    </div>
    <div class="search-bar">
        <input type="search" id="search" placeholder="Rechercher un jeu...">
        <button id="search-btn"><i class="fas fa-search"></i></button>
    </div>
    <div class="user-info">
        <% if (user) { %>
            <div class="profile-section">
                <div class="info"><p><%= user.username %></p>
                    <button id="logout-btn">Déconnexion</button></div>
                <div class="photo"><img src="<%= user.profile_pic %>" alt="Photo de profil" class="profile-pic"></div>
                
            </div>
        <% } else { %>
            <button id="open-login-btn">Se connecter</button>
        <% } %>        
    </div>
</nav>
